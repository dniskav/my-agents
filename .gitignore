# deps
node_modules/
package-lock.json
bun.lock

# runtime / session artifacts
.tmp/
*.log

# editor / OS
.DS_Store
**/.DS_Store

# third-party document skills (fonts/PDFs with their own licenses, not part of the squad)
skills/

# local-only opencode state
.agent-mode
.rimuru/
# auth.json   (uncomment if this exists on your machine)
